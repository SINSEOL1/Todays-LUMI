<Window x:Class="TodaysLUMI.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="218"
        Height="66"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize">
    <Border x:Name="MainBorder"
            Background="#EA15171B"
            BorderBrush="#352A2F37"
            BorderThickness="1"
            CornerRadius="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border x:Name="AccentBar"
                    Grid.Column="0"
                    Background="#405A86"
                    CornerRadius="5,0,0,5"/>

            <StackPanel x:Name="ContentStack"
                        Grid.Column="1"
                        Margin="14,9,12,8">
                <TextBlock x:Name="TitleText"
                           Text="오늘의 루미"
                           FontSize="11"
                           Foreground="#959CA6"/>
                <TextBlock x:Name="ItemNameText"
                           Text="운석"
                           Margin="0,2,0,0"
                           FontSize="17"
                           FontWeight="SemiBold"
                           Foreground="#F3F5F7"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
